<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    x:Class="MyStreamTimer.WinUI.Views.WelcomeBackDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="Welcome back to My Stream Timer"
    AutomationProperties.AutomationId="WelcomeBackDialog"
    DefaultButton="Primary"
    PrimaryButtonText="Get started"
    SecondaryButtonText="Learn about Pro"
    Style="{StaticResource DefaultContentDialogStyle}"
    mc:Ignorable="d">

    <StackPanel MaxWidth="440" Spacing="20">
        <TextBlock
            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
            Style="{StaticResource BodyTextBlockStyle}"
            Text="Version 3 is a ground-up rebuild for Windows 11. Your timers, formats, file names and Pro unlock are exactly where you left them — here's what's new."
            TextWrapping="Wrap" />

        <Grid ColumnSpacing="16" RowSpacing="16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Border
                Width="40"
                Height="40"
                Background="{ThemeResource AccentFillColorDefaultBrush}"
                CornerRadius="8">
                <FontIcon Foreground="{ThemeResource TextOnAccentFillColorPrimaryBrush}" Glyph="&#xE771;" />
            </Border>
            <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="2">
                <TextBlock Style="{StaticResource BodyStrongTextBlockStyle}" Text="Fresh redesign" />
                <TextBlock
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    Style="{StaticResource CaptionTextBlockStyle}"
                    Text="A native Fluent interface built on WinUI 3, with a sidebar for every timer and Mica that blends into your desktop."
                    TextWrapping="Wrap" />
            </StackPanel>

            <Border
                Grid.Row="1"
                Width="40"
                Height="40"
                Background="{ThemeResource AccentFillColorDefaultBrush}"
                CornerRadius="8">
                <FontIcon Foreground="{ThemeResource TextOnAccentFillColorPrimaryBrush}" Glyph="&#xE790;" />
            </Border>
            <StackPanel
                Grid.Row="1"
                Grid.Column="1"
                VerticalAlignment="Center"
                Spacing="2">
                <TextBlock Style="{StaticResource BodyStrongTextBlockStyle}" Text="Themes" />
                <TextBlock
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    Style="{StaticResource CaptionTextBlockStyle}"
                    Text="Follow Windows, or choose Light or Dark from Settings."
                    TextWrapping="Wrap" />
            </StackPanel>

            <Border
                Grid.Row="2"
                Width="40"
                Height="40"
                Background="{ThemeResource AccentFillColorDefaultBrush}"
                CornerRadius="8">
                <FontIcon Foreground="{ThemeResource TextOnAccentFillColorPrimaryBrush}" Glyph="&#xE8A7;" />
            </Border>
            <StackPanel
                Grid.Row="2"
                Grid.Column="1"
                VerticalAlignment="Center"
                Spacing="2">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <TextBlock Style="{StaticResource BodyStrongTextBlockStyle}" Text="Pop-out timer previews" />
                    <Border
                        Padding="8,1"
                        VerticalAlignment="Center"
                        Background="{ThemeResource AccentFillColorDefaultBrush}"
                        CornerRadius="10">
                        <TextBlock
                            Foreground="{ThemeResource TextOnAccentFillColorPrimaryBrush}"
                            Style="{StaticResource CaptionTextBlockStyle}"
                            Text="Pro" />
                    </Border>
                </StackPanel>
                <TextBlock
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    Style="{StaticResource CaptionTextBlockStyle}"
                    Text="Float any timer in a compact always-on-top window with your own font and colours."
                    TextWrapping="Wrap" />
            </StackPanel>
        </Grid>
    </StackPanel>
</ContentDialog>
